{
  "name": "book-beacon",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm run build",
    "start": "cd server && node index.js",
    "dev": "cd server && node index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
